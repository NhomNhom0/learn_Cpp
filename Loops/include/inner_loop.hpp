#ifndef INNER_LOOP_HPP
#define INNER_LOOP_HPP
namespace NestedLoop
{
    void InnerLoopForward(int inner, int outer);
    void InnerLoopBackward(int inner, int outer);
    void InnerLoopInvert(int inner, int outer, int init);
}
#endif